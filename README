NuGet bits come from its default feed of packages.

This addin for MonoDevelop will be Apache Licensed as NuGet. I think this is compatible with both projects' licenses.

The idea is to have similar GUI (GTK# based) for what they have for VS2010, or better.
